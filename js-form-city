function () {
  var city =  document.querySelector ('[id="shippingAddress.city"]');
  return city.value;
  var parsed = str.normalize('NFD').replace(/([\u0300-\u036f]|[^0-9a-zA-Z])/g, '');
  return parsed;
}
