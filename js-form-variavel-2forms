function () {
  var primeiro =  document.querySelectorAll ('[id="variável"]')[0].value;
  var segundo =  document.querySelectorAll ('[id="variável"]')[1].value;
  if (primeiro) {var email = primeiro}
    else {var email = segundo};
  
  return email;
}
