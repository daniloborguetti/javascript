function () {
  var primeiro =  document.querySelectorAll ('[id="variavel"]')[0].value;
  var segundo =  document.querySelectorAll ('[id="variavel"]')[1].value;
  if (primeiro) {var email = primeiro}
    else {var email = segundo};
  
  return email;
}
