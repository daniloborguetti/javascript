function() {
  var products = {{dlv matriz items}}; // Substitua pela variável que abre a matriz items
  return products.reduce(function(arr, prod) { return arr.concat(prod.item_name); }, []).join(); // Verifique o formato do parâmetro name
}
