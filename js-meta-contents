function(){
  return {{dlv matriz items}}.map(function(product){ // Substitua pela variável que abre a matriz items
    return {
      id: product.item_id, // Verifique o formato do parâmetro id
      quantity: product.quantity, // Verifique o formato do parâmetro quantity
    }
    })
}
