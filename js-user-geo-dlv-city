function() {
  var dlvCity = {{dlv-city}}; // Substitua pela variável da sua Camada de Dados
  if (typeof dlvCity === 'string') {
    
    // Converte para letras minúsculas e remove espaços
    return dlvCity.toLowerCase().replace(/\s/g, '');
  }
  return undefined;
}
