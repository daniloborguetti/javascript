function() {
total_produtos = 0;
data = {{dlv até a matriz de dados}};
$.each( meus_dados, function(index) {
total_produtos += (parseFloat(data[index].price) * parseFloat(data[index].quantity));
});
return total_produtos;
}
