function () {
  if ({{input-variavel}}) {
    
    // Se tiver informação no DataLayer executa, senão proximo
    return {{input-variavel}};
  
  } else if ({{cookie-variavel}}) {
    
    // Se tiver informação nos Cookies executa, senão vazio
    return {{cookie-variavel}};

  } else {
    return undefined;
  }
}
