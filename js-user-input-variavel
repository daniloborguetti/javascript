function inputVariavel() {

  // Substitua pela variável do seu formulário
  var nameField = document.querySelector('[id="variavel"]'); 

  if (nameField) {
    var name = nameField.value;

    return name;
  } else {
    return undefined;
  }
}
