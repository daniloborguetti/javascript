function() {
  var productsId = {{js-google-content_ids}}; // Substitua pela variável google-content_ids
  return ecommerceProductData = productsId.map(function(id) {
    return{
      id: item_id,  // Verifique o formato do parâmetro id
      google_business_vertical: 'retail',    
    }
  });
}
