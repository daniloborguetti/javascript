function(){
  return {{dlv matriz items}}.map(function(product){
    return {
      content_id: product.item_id,
      content_name: product.item_name,
      quantity: product.quantity,
      price: product.item_price,
    }
    })
}
